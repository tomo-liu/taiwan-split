@AGENTS.md

# taiwan-split プロジェクト概要

## アプリ概要

台湾人向けの割り勘・精算Webアプリ（練習作品）

## ターゲットユーザー

台湾人の友達グループ（3〜10人程度）

## 解決する問題

- 旅行・食事での割り勘をLINEでやり取りする手間をなくす
- 誰が何を払ったか管理して、最後に最小振込数で精算する

## 技術スタック

- Next.js (App Router) + TypeScript
- Supabase（DB・リアルタイム同期）
- Tailwind CSS

## 対応言語

- 繁体字中国語（デフォルト）
- 英語
- 将来的に多言語対応も追加予定

## 主要な設計方針

- アカウント登録不要
- グループ作成後にURLを発行、LINEでシェアして参加
- 参加者は名前を入力するだけ（同一デバイスからの再登録は制限）
- 名前が重複してもアイコン・色で視覚的に区別
- 費用追加時に「誰と割るか」を毎回選択できる（後から参加者を過去の費用に追加も可能）
- 精算は最小振込数で自動計算

## 画面構成

1. トップページ：グループ名入力 → グループ作成
2. グループページ（/group/[id]）：URL共有・メンバー一覧・費用リスト
3. 参加ページ：名前入力 → グループ参加
4. 費用追加モーダル：金額・説明・支払者・割り勘メンバー選択
5. 精算ページ：誰が誰にいくら払うかの一覧

## 開発フェーズ

- Phase 1: セットアップ・DB設計 ✅完了
- Phase 2: グループ作成・URL共有　✅完了
- Phase 3: メンバー参加　✅完了
- Phase 4: 費用追加・記録 ✅完了
- Phase 5: 精算計算 ✅完了
- Phase 6: UI・言語切り替え ✅完了

## コーディング規則

- コンポーネントはcomponents/フォルダに分けて作成
- 型定義はtypes/index.tsにまとめる
- 一度に大量のコードを書かない（フェーズごとに完成させてから次へ）
- エラーハンドリングを必ず入れる
